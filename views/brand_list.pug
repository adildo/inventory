extends layout

block content
    h1= title
    
    ul
        each brand in brand_list
            li
                a(href=brand.url) #{brand.name}
                if brand.description
                    p - #{brand.description} -
                else 
                    p -   n/a    -
        else
            li There are no Items